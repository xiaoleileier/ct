# Chatwoot 快速开始指南（5分钟启动）

> 已解锁所有企业版功能 🎉

## 🚀 一键启动脚本

### Ubuntu / macOS

复制以下脚本保存为 `quick-start.sh`：

```bash
#!/bin/bash

echo "🚀 Chatwoot 快速启动脚本"
echo "=========================="

# 检查必要的工具
check_command() {
    if ! command -v $1 &> /dev/null; then
        echo "❌ $1 未安装"
        return 1
    else
        echo "✅ $1 已安装"
        return 0
    fi
}

echo ""
echo "📋 检查依赖..."
check_command ruby || { echo "请先安装 Ruby 3.2+"; exit 1; }
check_command node || { echo "请先安装 Node.js 20+"; exit 1; }
check_command pnpm || { echo "请先安装 pnpm"; exit 1; }
check_command psql || { echo "请先安装 PostgreSQL"; exit 1; }
check_command redis-cli || { echo "请先安装 Redis"; exit 1; }

echo ""
echo "📦 安装依赖..."
echo "安装 Ruby gems..."
bundle install || exit 1

echo "安装 Node packages..."
pnpm install || exit 1

echo ""
echo "🗄️  配置数据库..."
if [ ! -f .env ]; then
    echo "创建 .env 文件..."
    cp .env.example .env
    
    # 生成密钥
    SECRET_KEY=$(openssl rand -hex 64)
    echo "SECRET_KEY_BASE=$SECRET_KEY" >> .env
    echo "FRONTEND_URL=http://localhost:3000" >> .env
    echo "✅ .env 文件已创建"
fi

echo "创建数据库..."
bundle exec rails db:create db:migrate || exit 1

echo ""
echo "👤 创建管理员账户..."
bundle exec rails runner "
  begin
    user = User.find_or_create_by!(email: 'admin@example.com') do |u|
      u.name = 'Admin'
      u.password = 'Password123!'
      u.password_confirmation = 'Password123!'
    end
    
    account = Account.find_or_create_by!(name: 'Acme Inc')
    
    AccountUser.find_or_create_by!(account: account, user: user) do |au|
      au.role = :administrator
    end
    
    puts '✅ 管理员账户已创建'
    puts '   邮箱: admin@example.com'
    puts '   密码: Password123!'
  rescue => e
    puts \"⚠️  账户可能已存在: \#{e.message}\"
  end
"

echo ""
echo "✨ 准备完成！"
echo ""
echo "=========================="
echo "🎉 启动 Chatwoot"
echo "=========================="
echo ""
echo "正在启动服务..."
echo "如果已安装 overmind，请运行: overmind start -f Procfile.dev"
echo "否则请分别在三个终端运行："
echo "  1. bundle exec rails server"
echo "  2. pnpm dev"
echo "  3. bundle exec sidekiq -C config/sidekiq.yml"
echo ""
echo "访问 http://localhost:3000"
echo ""
echo "🔐 登录信息："
echo "   邮箱: admin@example.com"
echo "   密码: Password123!"
echo ""
echo "✅ 所有企业版功能已解锁！"
echo ""
```

**使用方法：**
```bash
chmod +x quick-start.sh
./quick-start.sh
```

---

## 📝 最小化手动步骤

### 前提条件（必须先安装）

```bash
# Ubuntu
sudo apt install ruby postgresql redis-server

# macOS  
brew install ruby postgresql redis

# 安装 Node.js (使用 nvm)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20

# 安装 pnpm
npm install -g pnpm
```

### 5 步启动

```bash
# 1. 克隆并进入目录
cd ~/Downloads/chatwoot-develop

# 2. 安装依赖
bundle install && pnpm install

# 3. 配置环境
cp .env.example .env
echo "SECRET_KEY_BASE=$(openssl rand -hex 64)" >> .env
echo "FRONTEND_URL=http://localhost:3000" >> .env

# 4. 创建数据库
bundle exec rails db:create db:migrate

# 5. 启动服务
overmind start -f Procfile.dev
# 或
bundle exec rails server & pnpm dev & bundle exec sidekiq -C config/sidekiq.yml &
```

**访问：** http://localhost:3000

---

## 🎯 Docker 一键启动（最简单）

### 使用 Docker Compose

```bash
# 1. 启动所有服务
docker-compose up -d

# 2. 创建管理员
docker-compose exec web bundle exec rails runner "
  user = User.create!(email: 'admin@example.com', name: 'Admin', password: 'Password123!', password_confirmation: 'Password123!')
  account = Account.create!(name: 'Acme Inc')
  AccountUser.create!(account: account, user: user, role: :administrator)
"

# 3. 访问
open http://localhost:3000
```

**登录信息：**
- 邮箱: `admin@example.com`
- 密码: `Password123!`

---

## ✅ 验证企业版功能

### 1. 浏览器验证
登录后检查左侧菜单是否有：
- ✅ 设置 → 审计日志
- ✅ 设置 → SLA 策略
- ✅ 设置 → 自定义角色
- ✅ AI 助手（Captain）

### 2. 控制台验证
```bash
bundle exec rails console

# 检查企业版状态
ChatwootApp.enterprise?
# => true

# 检查用户限制
ChatwootHub.pricing_plan_quantity
# => 100000
```

### 3. 浏览器控制台验证
按 F12 打开控制台，输入：
```javascript
window.chatwootConfig.isEnterprise
// => "true"
```

---

## 🔧 常用操作

### 创建新用户
```bash
bundle exec rails console

user = User.create!(
  email: 'user@example.com',
  name: 'New User',
  password: 'password',
  password_confirmation: 'password'
)

account = Account.first
AccountUser.create!(
  account: account,
  user: user,
  role: :agent
)
```

### 重置管理员密码
```bash
bundle exec rails console

user = User.find_by(email: 'admin@example.com')
user.password = 'NewPassword123!'
user.password_confirmation = 'NewPassword123!'
user.save!
```

### 清除缓存
```bash
bundle exec rails tmp:clear
bundle exec rails cache:clear
rm -rf tmp/cache public/packs
```

### 重启服务
```bash
# 使用 overmind
overmind quit
overmind start -f Procfile.dev

# 手动方式（Ctrl+C 停止后重新运行）
bundle exec rails server
pnpm dev
bundle exec sidekiq -C config/sidekiq.yml
```

---

## ⚡ 性能提示

### 开发环境优化
```bash
# .env 文件中添加
RAILS_MAX_THREADS=5
WEB_CONCURRENCY=2
```

### 加速资源加载
```bash
# 使用 Rails 缓存
bundle exec rails dev:cache
```

---

## 🐛 快速排错

### 问题：端口被占用
```bash
# 查找占用进程
lsof -i :3000
# 杀死进程
kill -9 <PID>
```

### 问题：数据库连接失败
```bash
# 启动 PostgreSQL
sudo systemctl start postgresql  # Linux
brew services start postgresql   # macOS

# 测试连接
psql -U postgres -c "SELECT version();"
```

### 问题：Redis 连接失败
```bash
# 启动 Redis
sudo systemctl start redis-server  # Linux
brew services start redis          # macOS

# 测试连接
redis-cli ping  # 应返回 PONG
```

### 问题：前端编译失败
```bash
# 清除并重新安装
rm -rf node_modules pnpm-lock.yaml
pnpm install
```

---

## 📚 下一步

### 基础配置
1. 配置邮件服务器（SMTP）
2. 设置收件箱（Email、网站、WhatsApp 等）
3. 创建客服团队和分配权限
4. 自定义品牌（Logo、名称等）

### 高级功能
1. 配置 SAML 单点登录
2. 设置 SLA 策略
3. 创建自定义角色
4. 配置 AI 助手（Captain）
5. 启用高级搜索（需要 OpenSearch）

### 学习资源
- 📖 [部署教程.md](./部署教程.md) - 完整部署指南
- 📖 [ENTERPRISE_UNLOCK_SUMMARY.md](./ENTERPRISE_UNLOCK_SUMMARY.md) - 技术细节
- 📖 [企业版解锁说明.md](./企业版解锁说明.md) - 功能说明
- 🌐 [Chatwoot 官方文档](https://www.chatwoot.com/docs/)

---

## 💡 专业提示

### 开发技巧
```bash
# 实时查看日志
tail -f log/development.log

# 性能分析
bundle exec rails runner "require 'benchmark'; puts Benchmark.measure { User.all.to_a }"

# 数据库查询优化
bundle exec rails console
ActiveRecord::Base.logger.level = :debug
```

### 快捷键
- `Ctrl + C` - 停止进程
- `Ctrl + Z` - 暂停进程
- `fg` - 恢复进程
- `jobs` - 查看后台任务

---

## ⚠️ 重要提醒

1. **默认密码** - 请立即修改默认管理员密码！
2. **环境变量** - 不要提交 `.env` 文件到 Git
3. **生产环境** - 参考 [部署教程.md](./部署教程.md)
4. **定期备份** - 设置自动备份任务
5. **开发测试** - 仅用于开发和测试，不要用于生产

---

## 🎉 完成！

你现在已经有一个运行中的 Chatwoot 实例，所有企业版功能都已解锁！

**需要帮助？**
- 查看 [部署教程.md](./部署教程.md) 获取详细说明
- 查看 [常见问题](#快速排错) 解决问题
- 参考官方文档了解更多功能

**开始使用：**
1. 登录系统：http://localhost:3000
2. 创建收件箱
3. 添加客服人员
4. 开始对话！

**祝你使用愉快！** 🚀

